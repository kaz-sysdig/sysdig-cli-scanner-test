# .github/workflows/test.yml
name: Test Dispatch

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - name: Echo Hello
        run: echo "Hello from GitHub Actions"

